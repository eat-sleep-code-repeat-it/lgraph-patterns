Submissions Viewer
Files
submission_2026-06-04_02-27-48_f82490c8.md
submission_2026-06-05_17-36-01_0c3ccbaf.md
submission_2026-06-08_16-11-42_d43429a7.md
submission_2026-06-08_16-13-01_8fcc0fee.md
submission_2026-06-08_18-40-51_858b6b8a.md
submission_2026-06-08_20-08-53_1f42dced.md
submission_2026-06-08_20-14-53_4c6f5efb.md
Content
submission_2026-06-08_16-13-01_8fcc0fee.md
Memory-augmented and Persistent Agents
Why Is Memory Essential for Agentic Behavior?
13m 45s
Why Does Memory Matter?
AI agents are used to perform tasks and achieve shared and collective goals on behalf of users, but the capabilities and performance of AI agents may be somewhat limited without proper memory management. In this course, you will learn why memory is essential for effective agentic behavior. Starting with, why does memory management matter in agentic AI? AI agents are intelligence agents designed to operate within complex environments based on external inputs. Previously, you learned how agentic systems work. So they work by receiving an external input or information coming from the outside, which is perceived by the agents using sensors. So the sensors are like the eyes and ears of the agents, which are used to receive a message, a click, a request, or new data. The environment is the world where the agent operates to make decisions. So the environment can be digital like an app, a website, or servers, or it can be physical with robots, smart devices, similar to the Internet of Things, or through tools, databases, or even API calls. Then the agent uses actuators to take actions, to respond according to this environment. So they're like commands to take actions based on what is in the world now, in the current environment, in order to take the next actions and trigger the workflow. And so the agent's workflow is a series of steps orchestrated with multiple agents working together, collaborating to perform one task, to get something done, and achieve a shared and collective goal. So now let's see one quick example of a multi‑agent workflow. We can see that they are tied and connected together to perform one task. And very often they are connected to external tools or any other external system, and their control flow is frequently driven by large language models. But despite being powerful, we know that language models also have the limitations. And so the problem is that language models do not have real‑time awareness. So it is the same for the AI agents, which are tied to the same capabilities and limitations of the language models because they don't have a proper memory management or access to external knowledge. In this course, you will learn why memory is essential. We're going to explore why agents need memory, understand the limitations of stateless language models interactions with examples of popular agentic frameworks. And as a final project, you will learn to design a memory augmented persistence agent system that can remember, maintain context, learn from past interactions, and also personalize agentic behavior across sessions. This course is part of the Agentic Language Models for Developers learning path as continuation for teaching the development of agentic systems. And to make the best of this course, you should be familiar with the basics of vector databases and retrieval‑augmented generation, and also have experience with at least one agentic framework, like LangChain, LlamaIndex, or CrewAI, to name a few. By the end of this course, you will be able to build and deploy advanced multi‑agent systems with persistent and augmented memory.
Stateless vs. Memory-augmented AI Agents
Either stateless or memory‑augmented AI agents, the design choices that you make will directly affect how an AI agent works. An AI agent will perceive, sense, reason and act upon its current environment and external inputs. At a very high level, an AI agent follows a simple loop, very much like a person in a room, to make a very simple analogy. With sensors, like human eyes and ears, the agent perceives external information through inputs, tools, or data sources. Then the agent is trained to process percepts, to reason and decide what to do next. And similar to hands and feet, the agent is using the actuators to take actions or to chain multiple steps in the workflow to achieve one goal within an environment which can be digital or physical. The simplest form of a stateless AI agent can be a simple reflex agent that operates without any memory or internal states. It acts purely on immediate percepts to make decisions and actions driven by fixed and strict condition‑action rules. So with no memory, stateless AI agents only respond to current input and forget about everything else. With limited knowledge, it relies only on current percepts. And with no memory, it ignores history with no recollection of past interactions. It also doesn't adapt to environment changes with a behavior which is controlled and fixed only by predefined condition‑action rules. So now let's look at one simple stateless agent. So this is a simple reflex agent example with no memory that reacts only on the current percept using fixed condition‑action rules. We can see that this is a very predictable behavior based on two possible conditions. The agent is going to take the next action, which is either to clean or do nothing, so the condition can be either dirty or clean. So we're going to run this to define these agents with condition‑action rules. And next we have one array which is defined, that includes different conditions, either dirty, clean, and then dirty again. And based on the condition, we're going to see what is the next action taken by the agent, so we're going to run this. Here we go. So, we can see that based on the percept, which is dirty, the action will be to clean. Another example which is with the clean condition, the next action for the agents is to do nothing, and so forth. So this is an example of a simple stateless reflex agent that only reacts on the current percepts, which is also very predictable based on these condition‑action rules. In contrast, memory‑enabled AI agents are designed to retain and learn from prior information and interactions, allowing them to reason more effectively, adapt their behavior, and also produce more effective and context‑aware outputs with tools and memory, tools to extend the agent's capabilities beyond the base model and memory to add context and learning capabilities across interactions. So, the two combined together allows to optimize decision‑making and produce augmented and higher‑quality outputs. And so the key differences between stateless and memory‑augmented AI agents is that inputs in stateless AI agents are treated independently with no memory of past interactions with a simple architecture design. It doesn't adapt to environment changes, so it is very predictable, allowing for faster and simple outputs because of fixed condition‑action rules. In contrast, memory‑augmented AI agents can make decisions based on past interactions. It often requires a more complex architecture with context and memory management. It also allows for a more adaptive and dynamic behavior with slower, but also more accurate and nuanced outputs. It is also very flexible and customizable, which allows overall for a more effective and powerful agentic behavior.
Types of Memory in AI Agents
Different types of memory in AI agents serve different roles in decision‑making. So let's discover together each one of the three memory types in agentic AI. First, you have episodic, then semantic, and finally, procedural. We're going to discover each one of them with examples. The episodic memory refers to the memory of everyday events. It refers to past experiences and events in prior conversations. It stores specific experiences or events tied to time, place, and context. It is the memory of events and episodes of life, more specifically, personal past events, like a birthday or a party with family and friends on the 4th of July. Then we have semantic memory. That stores general knowledge and rules. It refers to general facts, domain, and common concepts, and we have examples like the 4th of July, which is commonly and globally known as the Independence Day in the United States. So, we all know that this is a well‑established historical fact, just like the Bastille Day in France on the 14th of July. Now let's make a side‑by‑side comparison of these two types of memory. So first, the episodic memory that refers to specific episodes and personal experience. So we know that this is personal and contextual. This is related to what happened to me or to you specifically. So you have examples of episodic events that can be memorized, like yesterday I googled sci‑fi movie titles, or last week, my car broke down. In contrast, on the right, you have general knowledge. That represents semantic memory. This refers to the memory shared by all, by everyone. It is common and noncontextual, meaning that this is the total opposite of episodic and personal events. So you have examples like pandas is known as a Python library in the dev community, and sci‑fi is what we call a movie genre. Let's look at another example of general knowledge in the dev community. That refers to semantic memory with LangChain, which is largely known as a software framework to build applications driven by large language models, and it was released in October 2022. This is something that we know for a fact that we can also check on this Wikipedia page. And we can also learn here as a general fact that it is also used to build stateful AI agents using LangGraph, and this platform was released, launched on the 14th of May in 2025. So this can be remembered as a general fact that will be stored in the semantic memory. Third and last, you have what we call procedural memory. That remembers how to perform tasks. It represents how to do things. This is the memory of skills and procedures. For example, you learn how to cook hot dogs. You learn and remember how to ride a bike. You also have the memory of the procedure and the sequences to build AI agents systems. It's like when you train a model to perform a task or a skill, that knowledge is stored in its procedural memory, allowing the models to recall and execute the learned actions and sequences. Let's do again a side‑by‑side comparison with the three memory types, and this time we want to compare and highlight what is procedural memory compared to the two first episodic and semantic memories. So what you need to keep in mind is that the procedural memory represents. Knowledge of how to do things. This is the memory of skills and capabilities, like knowing how to ride a bike or writing code. So, stateless language models can generate intelligent responses, but they do not learn or adapt over time unless memory mechanisms are explicitly introduced and applied to allow agents to learn from past interactions, adapt to new situations, and also maintain continuity in between conversations and sessions.
Memory Management in AI Systems: Limitations and Strategies
Memory management is critical in AI systems. Because language models do not have real‑time awareness, by default they are stateless with no memory. They usually operate within a fixed context window with no persistent memory, leading to slow API responses, no control over token usage, and higher costs, which means poor reasoning, weak workflow continuity, and adaptability. So, to overcome these limitations, memory is essential in agentic AI. So, memory management is critical and a core component of the agentic systems for building stateful and self‑improving learning agents that can retain context, adapt and generate personalized answers, and also achieve shared and collective goals. Some of the strategies and techniques in order to implement an efficient and improve agentic behavior include short‑term and long‑term memory storage, storage summarization to optimize memory management, like memory pruning and caching, techniques like integrating RAG and tool calling in order to expand the capabilities of the base language models, and also hybrid memory methods to create self‑managed stateful memory augmented agents. So the typical use cases for agentic applications include conversational AI assistants, customer and technical support AI agents, multi‑step agentic automation, and also research AI workflow, to name a few. Up next, we're going to see multiple examples with some of the leading agentic frameworks with Semantic Kernel, Haystack, MemGPT and also LlamaIndex, and LangChain.
2
Comparing Techniques and Frameworks for Persistent Memory
41m 48s
Introducing the Leading Memory Persistence Frameworks
In the past few years, AI has changed the way that we work, create, and collaborate. Just a few years ago, the world was captivated by generative AI with systems and chatbots that can generate content, images, and even code, but that was only the beginning. We are now entering a new phase with agentic AI systems that can behave independently, make decisions, and take actions. In this module, you will learn about the techniques and the leading agentic frameworks to create stateful AI agents with persistence memory. Previously, you learned what is an agentic workflow with agents that act, decide, and take actions based on inputs, percepts, its environment, and also based on interactions with other systems. The next goal and project is to build an agentic system to help with a few coding projects. As a software developer, you implement and integrate multiple solutions every day at a very fast pace, and you have reached a point where you need to hire some help. You are also considering using AI to assist with a few coding tasks in order to increase efficiency, improve productivity, and also save time and resources. The problem that we're going to need to solve is that language models are stateless by default. And so persistence memory is required to maintain continuity between multiple agents and enable effective and reliable behavior over time. So your next goal and project is to build a multi‑agent system with persistence memory to turn stateless agents to stateful super agents with augmented memory. So in this module, we're going to use some of the leading agentic frameworks to help us put these projects together, starting with Semantic Kernel and Haystack to build production‑ready AI agents, MemGPT in order to create agents with memory blocks, and also LlamaIndex and LangChain, two popular agentic frameworks with built‑in, out‑of‑the‑box, ready‑to‑use components in order to implement effective agentic behavior by integrating persistence memory and allow AI agents to learn, remember, and improve over time.
Kernel: Managing Conversation History
The first example that we're looking at is with the Semantic Kernel framework. That provides a simple way to maintain and persist context of conversations using a thread. So the Semantic Kernel agent framework provides a platform to design AI agents to perform specific tasks, like coding tasks, and achieve specific goals independently based on one input. So let's go on the website to discover more about Semantic Kernel, and we're going to go straight to this section to discover how to create an agent and discover the different agent types, and we want to look at this one. So, for the next project, we want to create a ChatCompletionAgent, and we're going to look at the different steps. And let's just make sure that you select Python for the programming language. And the first step is to install the required packages and SDK. Next, we create the agents by providing with an AI service and provider. We provide with the name and instructions. So, the instructions is like the prompt to guide the agent's behavior, to tell the agent how to act. Then, we initialize the kernel. We can add the service to the kernel to create the agents. And so we can then see how to converse with the agents. And the interesting part is to see how we can maintain conversation history between the different interactions in order to create a learning agent. Just a quick reminder that this course is not designed to teach you how to implement an AI agent or agentic workflows from start to finish. We want to focus mainly on integrating persistence to create memory‑augmented AI agents. So I'm still going to walk you through the steps to create a learning agent, but we're not going to be looking at exactly how to create an agent from start to finish. And so for the next project, we start by creating a new kernel object. We initialize the kernel, then we add a new AI service to connect to an AI provider. Then we create the ChatCompletion agents. And to be authorized to interact with the language models, you need to set up an API key and then add it to your projects in the .env file. And so to begin with our next project, I'm going to take you to Visual Studio Code and open this notebook. So we're going to use Jupyter Notebook, which is very practical, user‑friendly, and convenient. So you don't need to go through any development environment setup. And it works the same for both Mac and Windows users, so that's very practical and convenient. So, for every demonstration, it's going to be like a mini learning guide with the steps to complete the project. So, I'm also adding some notes to understand the process and also links to the specific documentations. So, the goal for this project is to set up a persistence memory by defining a thread. You can run a block, a cell in order to see and read the output right below, and you also have the option to run everything at the same time. So let's go step‑by‑step. So first we want to install the Semantic Kernel package. You have all the steps right here. We're going to start with that, so it's going to ask me to select the Python environment, so let's do that. Next, we're going to load the environment variables. And also important in order to interact with the language models, you also need to add an API key to your project, and that is something that you're going to need to add to your .env file. So, remember to go to the OpenAI API platform to create a new secret key and then add it to your .env file in order to be authenticated and authorized to connect with the language model. And so next we can create the ChatCompletion service using the OpenAI provider, then initialize the kernel service. So let's remember also to run every cell. All right, then we initialize the kernel and add the service to it, just like the project guide on the website. And then next we're going to create the ChatCompletionAgents. So, as a reminder to create an agent, we add the kernel objects with the AI service, we provide the agents with a name and instructions. So basically, we tell these agents to act as a senior developer assistant in order to help with coding tasks and debugging.
Kernel: Managing Conversation History – Part 2
So, what we've done so far is to initialize the kernel, add the AI service, we created an agent, and finally, we have seen how to manage the conversation history by defining a thread. And so this is how a typical flow looks like when we run an agent. So we submit a request to these agents that can connect to a language model, to an AI service, and the same agents can also connect to external systems and APIs to enhance the response before generating a response. And in the next example, we're going to see also the same agents accessing chat history using a thread in order to generate responses based on previous inputs, based on previous conversations. So now we're going to see the same example, see how we can manage the conversation history by using the same thread, and see if the agent is capable of processing new requests based on the conversation history. Finally, we go to the interesting part, which is to learn to manage conversation history with this agent. So, let's go back to the documentations for a moment to understand how to proceed. And to create a new thread, we're going to go find this section, which is with ChatHistoryAgentThread. So I'm going to go ahead and copy this line. So now at this exact location, we're going to add a new thread object using the ChatHistoryAgentThread class. And that is always a good practice to provide with one specific ID. So I'm going to choose one very simple and generic ID name like this, and we're going to use the same thread objects to hold the conversation's context over every conversation in order to retain history between interactions. And so we're going to pass the thread objects to the agents to create a learning agent. And so right below we're going to have a method to print the thread summary. So we're going to run this. So remember also to run this cell. All right, so it's done. And right below, it is interesting because we're using also some mock tests. So we're going to use this mock test to test how history, the conversation history, is maintained over conversations. So that is always a good practice to write and run some tests to check your application is behaving and performing as expected. So basically we test how we save to the thread. We also test the number of messages that are retrieved and check if they are properly retrieved. And we also test by submitting some tests and requests to the agents to see if we can find the same history by using these assertions. So we check basically if we can find a task about JSON or the CSV task. We test if we can find the messages that were submitted to the agents in the same conversations context. So we test if the messages are properly saved to the context and properly retrieved. So let's run these tests to see if the agent is performing as expected by being able to remember from previous conversations. And we're going to see the output right below. So we have a first request, which is to ask the agents to write the Python functions to convert a string into a URL‑friendly slug. So that is the first task request. We can see the full outputs with the rest of the requests and answers. We can click here in the text editor to see the full output. So we have a first request, which is to write a Python function to convert a string into a URL‑friendly slug. And next, you're going to see that the next request is to ask the same agents to refactor the previous Python functions to improve performance and readability. So, basically we check and test if the agent is capable of performing based on the current input, but also based on the previous input, if it can remember the previous input and then perform based on the previous inputs. And we can see the answer, which is that the above function is pretty optimal. So it can remember that how was processed the previous request, and then it's going to give an answer based on the request and the previous conversations. So we know that this is working as expected. So we have an agent that can learn, that can remember, and that can use the context from previous conversations. All right, so let's run it. I'm going to ask actually a question very similar to what we have here from these examples. I'm going to ask to write a Python function, and this time I'm going to ask to write a Python function to remove white space. Okay, so let's see. All right, so we're going to see the output here. We're going to wait for the response to be generated. All right, let's see the full output by clicking here in the text editor. Now we can read the answer from the assistants. To remove white space from a string, it's going to write the answer using Python. And right after we have asked to also print the thread summary, so we can see the previous messages, like the first request that was submitted. And all the way to the bottom, we should read the new request that we have just submitted. Here we go, which is to write a Python function to remove white space. Now let's ask a new question, which is to refactor. Refactor the code to use JavaScript this time. We just want to test if this agent is capable of learning from previous inputs, so let's go back. We're going to see the full output once it is finished. We can see the current request. So we have the first request, which was to write a Python function to remove white space. We have the thread summary. Once it is done, and it's fine, actually, I'm going to look it up. I'm just going to look up JavaScript because now we have a big output. And after that, the next request was to ask to refactor the code by using JavaScript. All right, and the answer right away is that, sure, I can refactor the Python function into JavaScript functions to remove white space. Okay, so that's great. So now we have a learning agent capable of performing and processing requests based on the context, which is capable of maintaining context, but also to perform process requests based on previous interactions. So, that is great. So the ChatHistoryAgentThread is a very simple way to enable history persistence over time, to maintain continuity and context across conversations, which helps AI systems and agents to learn and improve beyond the simple one‑off tasks. And this allows to move AI from a simple tool capable of writing code on‑demand to a system that can build upon experience, upon previous experience.
Using a Memory Store in AI Systems (Mem0)
By default, language models are stateless and do not have real‑time awareness. They have limited context Windows, they forget about previous conversations, and they don't have the ability to manage long‑term memory. So, adding and using a memory store in your AI systems can be the required step in order to create memory‑persistent AI agents. So, agents are driven by language models, so they're going to behave and generate a response based on its current environment, inputs, and percepts. So for every new request, the agent is going to generate a response based on the current inputs only and forget about the previous conversations. So in order to move from stateless to stateful agents that can actually learn, we're going to add a memory layer to allow continuity and self‑improvements between interactions. So the agents can save to the memory layer and also retrieve memories from past conversations, also from the memory layer. So it is the same for multi‑agent AI systems. Agents can share context between interactions in order to create self‑improving and adaptive AI systems. So Mem0 is what we're going to use in the next project. Mem0 allows to add and manage a memory layer designed for AI applications and agentic systems. The goal is to enable AI agents to remember, retrieve, and learn from past interactions, allowing AI agents to improve over time instead of starting from scratch with every request. So, in one sentence, we want to turn AI from one‑time responder into a system that can accumulate intelligence and knowledge over time.
Getting Started with Mem0
Now we look at the steps to get started with Mem0, so we want to create first an account, then create a new API key that we'll need to then set up in our next project. So, we'll need to provide with the Mem0 API key so we can interact with the Memorystore and also provide with a MEM0_USER_ID. And this will be used to label memories and messages that you save to the Memorystore, which will allow to structure the messages saved to the Memorystore as well and provide with an organized way to retrieve messages. It is like a namespace to structure history in your Memorystore. So to get started with Mem0, so this is very simple, you simply need to follow instructions and the steps on the screen, and you have two options to create an account, then authenticate. So I'm going to go ahead and authenticate with Google because I already have an account with Mem0. And once you are connected, you're going to land on this dashboard where you can see requests that were submitted. So the requests are like operations that were done using one Memorystore, so you can see operations like adding to the store or searching from the store. You can trace the different operations processed, but also the latency, which is good because you can actually see how performance is your Memorystore for your AI system. And we're using entities, so it's like namespace that you use to label and categorize history information, and you can see the different entities that are created and used in your Memorystore. So it's like a namespace that you use to label and categorize history information to regroup memories in your Memorystore. Let's go back to the dashboard. So, from the same page, you can see integration examples by initializing a new client. So how to search from this Memorystore and how to add to the Memorystore. But before doing that, let's go back to the dashboard and find the sections for the API keys. You'll need to set up an API key to be authorized to interact with your Memorystore, so that is an important step. So you'll see that this is very straightforward and simple. You simply need to provide with a name and then click on Create API Key. So once it is done, you can then go back to your projects. And just like you have done for your OpenAI secret key, you need to add these two environment variables, the MEM0_API_KEY, but also the user ID to categorize information and memories saved to the store. And once you're all set, we are ready to get started with a new project example using a memory layer with Mem0.
Building a RAG Agent with Memory Awareness
For this new project, we want to build a RAG agent with memory awareness. So we want to build an agent which is context‑aware thanks to RAG and that can also remember things by remembering past interactions. So we're going to use Mem0 to set up a Memorystore so we can save memories and also extract and retrieve from the Memorystore. And we're going to use Haystack, which is an open‑source AI orchestration framework to build production‑ready AI agents. So the steps to build this super AI agent integrated with RAG and memory awareness, we're going to first install the required packages, then set up an API key. That's going to be for Mem0 that we have done previously. We're going to see how to set it up. And then create and initialize a Mem0 client to set up a Memorystore. Then we're going to see examples to save and retrieve memories from this Memorystore. To get started with Haystack, you can follow this link and see how to get started quickly with a first basic agent example with Haystack. And we're going to go straight to learn in cookbook to find one example to learn to use Mem0 Memorystore with Haystack agents. And so the steps will be similar to our next example, which is to first set up a Memorystore, then create an agent that uses the Memorystore, then we can see examples of how to run the agents, and then generate personalized and augmented responses. So let's find our example. Here, we're using Jupyter Notebook in order to integrate RAG and memory and create a Haystack agent. To integrate RAG, so the process is simple, we set up a document vector store. So basically, the process is to first load data to index documents and then store it so the agents can access custom and personalized information in order to generate augmented responses. And the purpose here is to allow the agents to access guidelines, requirements, and coding standards in order to process coding requests based on specific requirements and coding standards. All right, and next we create functions, and this is just to help proceed with retrieving the information, so from the Memorystore or from the vector store. So, we build an agent which is capable of retrieving custom information based on specific guidelines, but also retrieve information from past conversation. Then we create the tools because for this example, we want to equip agents with tools in order to leverage the base capabilities and allow these agents to access the data, external information. So we have a memory_tool and standards_tool. So this agent is stateful because it's going to be able to retrieve information and generate responses based on past engineering issues and also generate responses based on specific coding standards and best practices. And so we finally create the agents with one language model and a prompt. We want these agents to act as a senior engineering specialist. All right, so let's see one example. But before that, we need to set up the Memorystore. Let's go back here because we need to first initialize the clients, provide with filters, and also provide with a user ID. So I'm going to go ahead and here update with engineering_specialist because it's going to be a different user ID and namespace in order to save the new memories under this particular label. In order to set up the Memorystore, so we need to initialize a new client, and we're going to find this example on the Mem0 website documentation, so we're going to initialize it with an API key. We're going to name it simply memory, and it's going to be MemoryClient, and we specify the API key. So remember that you need to set it up here in the .env file. We're going to provide also with a user ID, so this time it's going to be engineering_specialists. And for the rest, I'm going to add the filters here, and we're going to use the filters to add to the payload every time that you send a request to the Mem0 API. So you need the filters so you can save the memories based on one specific user ID, a specific label or namespace as you'd like to name it, and also to retrieve memories. So below you have the helper functions that you can use to then store to the Memorystore and then retrieve the memories. And every time that you send a request, so that you post a request, you use the message, the actual object, memory object that you saved to the Memorystore, along with the MEMORY_USER_ID, and when you search, it's going to be based on a user input using also the filters. So let's try to run this. So we're going to go ahead and run all the cells for this Jupyter Notebook. So it may ask you to provide with the Mem0 API key. So that's okay, we're just going to find it in the .env file. And for the user ID, I changed it, so I'm going to provide with this one, so just make sure that you update it here. And I think that I should also change the way that it is set up in the project because we're not actually accessing it, that's going to be os like this, so we can access it from an environment variable. Okay, so we have all the cells that are being executed until we can actually ask the engineering agents personalized questions. So let's ask the agents to create an endpoint. We're going to ask, create an endpoint to upload files, we're going to say PDF files. Let's try that and see what is the output. All right, so now we have the output. Let's see what is the response. So, steps to create the upload endpoints. So this is using JavaScript for this particular example, Node.js and Express. We can see the full outputs by going to the text editor, and this time we're going to ask a different question. We're going to ask to do the same using Python language based on last requests, we're going to be more specific. Okay, so this is being processed. All right, so let's go back because I may need to open again. So now it can actually proceed with the request, actually in Python, and it knows that the request is to set up, create an endpoint for uploading PDF files based on the previous request. So that's good because it is using the coding standards, so we know that this agent is performing based on specific guidelines and coding standards. So this is working as expected with the integration of RAG. But also, it can remember from past interactions because I didn't need to be very specific with my request. I understood that the task was to create an endpoint to upload PDF files, but using Python, considering the following guidelines and best practices
MemGPT: Creating Agents with Memory Blocks
MemGPT is a memory‑augmented framework designed to create stateful agents that can remember, learn, and improve over time. It is designed to overcome the limitations of the large language models that operate without real‑time awareness and process requests within a fixed context window and token limits. That often leads to the loss of conversation history, latency, slow API responses, and a higher cost on every interaction. The underlying process with the MemGPT framework is that it works with a self‑managed memory control by separating memory management in two, with a working memory, that refers to the basic and short‑term memory, and long‑term memory. So, instead of managing the memory manually, the benefit with MemGPT is that it is self‑managed, knowing how to persist and process growing knowledge effectively. It enables the models to decide when the context is full, when to update the memory and how, by storing and retrieving relevant past interactions whenever it is necessary, and to ensure that memories are never lost. And so the core concept with MemGPT is that it helps turn stateless agents into self‑improving and self‑managed stateful agents that can learn and effectively retrieve information to generate personalized and augmented responses. Now, let's see how to get started and create a new agent with memory blocks. So, first you need to add an API key. Install the required packages and SDK. And finally, we see an example of how to create an agent and add some memories with memory blocks. So, the way it works is that every memory is divided into labeled blocks. And so each block includes a label and structured information that helps agents categorize and select memories based on whether this is from an agent or a user. That helps also differentiate between the different types of memories, between long‑term or working short memory. And we can also make the difference between the different memory types. So remember about the memory types, you have episodic, semantic, and procedural memory. That helps describe and understand how AI agents store and reuse knowledge and for what specific purpose. So for our next project, we're going to see how to initialize a new client with MemGPT, create an agent, and also create a shared memory block, and finally, see an example of usage to run and access the shared memory. And also, for your information, you also need to set up an API key. I'm going to walk you through the process before starting with the next project. So let's begin with our project to create agents with memory blocks, and we're going to go through every step and run every cell. So first, you need to install the required packages. Load the environment variable, we're going to get to that. Let's look at the next step, which is to create an agent. Of course, this example works like a mini guide, so you also have access to multiple resources and the documentation whenever you need. We save a memory block, which is labeled as a human, but also as persona, so we can categorize the information which is stored in the memory. Recreate an agent. And next, we can run some tests to retrieve stored information more specifically. So, we're going to also allow to run this. So let's go back up and run every cell. Then, load the environment variable. All right, so it's done. So about the API key, I also said that I was going to show you the steps. Very easy. You need to, of course, create a new account. And once it is done, you can go to this section, API keys, and then create a new key. By going there, then copy this key. Then go back to your projects to add it to your .env file. Once you're all set, you can go ahead and create a new agent. So that is the next step. So let me run this cell. Here you can see that we're adding memory blocks. That includes personal and contextual information about the user, and we also tell the agents to act as a senior AI engineer. And finally, we can run the agents, starting with some tests. We're going to ask about the personal information and see if the agent is capable of retrieving personal information about me. All right, so response from the agent, I know that your name is Sandy and that you work as a software engineer. Exactly how we have stored the information previously by using this memory block when we created the agent.
MemGPT: Creating Agents with Memory Blocks – Part 2
Now we can actually run it, so let's remove these comments and then test it. We need to here add this function to send new messages to generate the response, including retrieved information. So, let's run this. I'm going to ask to write a Python function to generate a random number between 1 and 10. And so the agents will automatically decide to update the memory if it is necessary, so we don't need to do it actually manually. So below here is the answer. That was very fast. So, here's a simple Python function to generate a random number between 1 and 10. And now I'm going to ask just between 1 and 100. So, I'm not going to be very specific with my request. We just expect actually the agents to remember the past interaction. All right, so here we can see that now, and I think that actually we should go to the text editor to see the full output, and here line 19, we can read here is the updated Python function to generate a random number between 1 and 100. That's great because now the agent is working internally, independently, and reflects internally to read from either the working memory or the relevant long‑term memory. And so the process is autonomous, self‑managed, adaptive, and also self‑improving in order to generate a response enhanced with retrieved knowledge. And to go beyond, some of the strategies and techniques that we can use to overcome the main pitfalls and limitations of the language models is to learn to implement both types of memory management, with short‑term and long‑term with hybrid search methods. Also, consider integrating long‑term memory storage with a vector database, integrating RAG in order to give the language models access to external and custom knowledge. Finally, using and implementing techniques like summarization and pruning whenever it is necessary in order to control the growing knowledge and avoid any memory loss, and in the long run, allow for low latency, better API responses, and control of the token usage and costs.
LlamaIndex: Building an Engineering AI Team
In this next and new project, we want to build a team of engineering AI agents that can achieve specific goals on behalf of users, that can learn and improve over time with memory‑augmented RAG, and also with the capability of sharing context across interactions. And we use LlamaIndex, an agentic framework designed for building knowledge‑driven AI applications and workflows. As an AI engineer and lead developer, your role is to implement and integrate multiple solutions at a fast pace. Your team often faces a heavy workload with floods of requests, and to save time and resources, you want to set up a team of AI agents to automate the same steps usually handled by developers. The goal is to design and orchestrate a multi‑agent workflow that can process code projects from start to finish and implement features in a timely fashion. Of course, you don't want a team of agents that simply write and review codes. You also need agents that can enforce coding standards, learn and improve over time. So, the purpose is to set up a team of stateful AI agents with context and memory awareness by improving and optimizing the setup with integration of a pipeline using RAG and setting up a vector store, and also enable persistence memory with long‑term storage using ChromaDB. We also consider applying techniques like summarization and pruning in order to optimize memory storage and management over time. This notebook project demonstrates how to build a collaborative AI engineering team that mirrors a real‑world software delivery workflow, and the system combines a multi‑agent collaboration with retrieval‑augmented generation to index and store documents in a vector store and allow retrieval of relevant documents using semantic search. And we also enable persistence memory, long‑term persistence memory, using ChromaDB. So that's something that we're going to be doing together. Below, we have a look at the multi‑agent workflow and architecture. So the engineering team consists of one developer in charge of first implementing the feature based on the task request, based on the user input. The next developer is in charge of refactoring and optimizing the code. Then, we have a reviewer. And finally, a lead developer with the role of approving the code before it goes to production. So the project goal is to create and automate a multi‑agent engineering team pipeline to process code projects from start to finish, from request to code approval. As a reminder, we're not building any projects, agentic projects, from scratch, so we can look at the setup to integrate RAG. So we have the prompts, we're going to go through every cell, so we have RAG, which is already integrated, and so the focus here will be to create and set up the memory storage with a ChromaDB client. So we're going to be doing that together. So we can go to the LlamaIndex documentation to discover how to set up a Chroma vector store. First, recreate a client using one vector store. So we're going to be using Chroma for the next example. All right, so we go to the step three to create the Chroma vector store. So we start with the creation of the clients and we create a collection. You can provide with a name, so the name that you'd like. Finally, we also create the vector store using the collection, then the storage context using this vector store. Coming next, what we create is an index. So we index any data that we save to the storage will be indexed. Then, we use this index to create a retriever, which will allow to run some search using semantic search. And once we are all set, we can then below discover the rest of the setup. So, optional, you can also run some functions to optimize memory storage and management. We're going to run this only when it is necessary to make sure that we keep the chat history to a minimum, the long‑term storage to a minimum, by running some pruning and summarization functions. And the purpose is to avoid overloading the context window of language models, which are working and operating within a fixed context window to enable latency, FastAPI responses, and also limit the token and API usage. And below we have some tools, functions, and we come to the step when we create agents with function agents. So this is coming from the LlamaIndex documentation, and we also build the workflow.
LlamaIndex: Building an Engineering AI Team – Part 2
And we can also test the memory. We have a few helper functions that we use to save, store to the memory, but also retrieve from the memory. So, we're going to run that. So, let's go back. And actually, what we're going to do is to, yeah, I'm going to go back up and make sure that we run every cell so we can actually use them. We can also, we have the options to run everything all at the same time. It's going to be much faster. And we can then check as we go through. So let's go back up. So first it's going to go through the setup, installation and setup of the packages. Load the environment variables. Sometimes it's going to stop and block at this stage when we set up the vector store for RAG. That's okay, you can just run here again because sometimes it has some issue just to point to the right documents, which is available here in your project, at the root of the project. Let's go back, and again, you can click on Run All to run all the cells altogether. All right, so it's going to go through every cell and execute every cell step‑by‑step. And here you have this step, which is interesting because we can test, we can actually test the memory by saving two test messages. And when we print the messages saved to the memory, we can see that we have a total of two messages and we can read the actual test messages. So, we know that this is working as expected. So below we also have some mock tests that we define to test pruning and summarization because this is the purpose of this project, which is to optimize memory management. So we're going to test that specifically. So what we do is to mock the user and agent messages. You decide how many messages that you want to keep in your storage. So if you exceed this maximum, it's going to reduce to this maximum number of messages. Everything before that is going to be deleted. So let's say that periodically you'd like to sanitize and clean up your storage every 30 days. In your team, for example, you're going to make sure that you always check that the number of messages is not going beyond this number and decide to delete any previous messages. Below you can see one example of the memory state before pruning with that many messages. So, basically what we've done here is to store 10 messages for this example, so we have exactly 10. Then it's going to run the pruning and summarization function to keep the number of messages to 5. And after that, we can count that we have exactly only 5 messages between the assistant and the user. All right, so we know that this is working as expected. Finally, what you can do is to actually test the vector storage yourself by adding some test messages, just like here. So what we've done is to add a higher number of messages, like 30. And because you want to keep only a number of 5 messages in the storage, it's going to prune the 25 older messages to only keep 5, and you can see that after pruning, you only have a count of 5 messages, which are saved in the storage. So below you can now test. It's going to be your turn, you can try it out and run some examples. For now, the function is commented out, so you can allow to run this one, and run some coding task examples. I'm going to show you this method, which is run_workflow, and the step will be to then run the conversation with the agentic team. Below, you're going to see that you can then save the messages every time that you submit a request. And also, run this prune memory function, which is going to be executed only when it is necessary, only when the number of messages stored in the vector store is beyond the number authorized and limited.
3
End-to-end Demo: A Multi-agent System with Persistent Memory
18m 3s
LangChain: Hybrid Memory Strategies and Techniques
In this last module, we build a multi‑agent AI system with persistent memory using LangChain and LangGraph. We need to build a collaborative and scalable AI system that employs hybrid memory strategies with short‑term and long‑term storage in order to maintain context of the sessions and share a high volume of reusable knowledge. Now your engineering team has been dealing with a growing number of tasks and requests, from feature development to bug fixes and code reviews, leading to an important backlog, and this has become really overwhelming for the entire team. So, to improve efficiency and productivity, we want to rely on one multi‑agent system. We also want to ensure reliability, scalability, and security. So, we need to build a system that can handle concurrent requests, a growing number of tasks, and we need a system that can easily overcome the limitations and constraints linked to the language models with optimization techniques and strategies. So, The goal is to build and orchestrate a multi‑agent workflow that integrates production‑grade key features with short‑term and long‑term memory. We want to implement memory pruning techniques with automated compression and summarization actions to reduce context overload and maintain performance. We also consider caching responses in order to reduce cost and latency and to create a system that is scalable, reliable, and easily deployable. So for this last project, we're using LangChain, which is a framework used for building applications driven by the large language models that allows to easily integrate tools and also memory features. Then we have LangGraph, the orchestration framework used for building and managing long‑running stateful agents. It extends LangChain, and it allows developers to build stateful multi‑agent system with complex workflows, but also with persistent memory. So, we're using both of LangChain and LangGraph in order to implement a multi‑agent system with persistent memory and to optimize memory management. So we're going to be using techniques to summarize and prune to make sure that we use memories that fit the limited language models context window and also caching to allow for FastAPI responses. Finally, we're going to add also observability to trace results and also performance. And one recommendation is always to run tests in order to check if your application is behaving as expected.
Maintaining Short-term Conversation History
The first step is to look at how to maintain short‑term conversation history with short‑term memory. So you're going to find instructions in the README file to create and activate a virtual environment. Next, you need to install the required packages and also set up the environment variables because we need an API key to interact with the language models. So now let's look at the projects inside Visual Studio Code, and the first thing that you want to look at is the README file to find instructions to get started. And so you're going to go through the setup and installation steps, starting with creating and activating a virtual environment. Next, installing the packages, and also set up API keys for the OpenAI secret key, and later we're going to look at how to add observability with LangSmith. Below, you have the steps for the project, and we start with memory storage and maintenance. First, what we want is to provide with a checkpointer to handle multi‑thread conversations. So, to maintain short conversation history between multiple agents, and also how to maintain history at a thread level. So before the practical part, so you can always look at the documentations, so in order to add short‑term memory, thread‑level persistence, you can set up and define a checkpointer, which is then used at the graph level. But we're going to use it at the agent level because we are building a multi‑agent system using LangGraph. Let's see how to do it. For your convenience, I'm providing with the code already set up that you can use to then complete the project. I'm going to copy these lines, and actually, it's going to be here. I'm going to put that here, and we're going to copy those two lines. And then go back to this utils file. And for every step, you're going to find here how to set up memory storage, starting with short‑term. You're going to see how to enable memory storage, starting with short‑term in order to define thread‑level persistence. And after that, you're going to have a look right below, you're going to see that for the tools, actually, it's going to be here for the tools because we define agents to use as tools, and when we define the agents, here you're going to see that we're using a checkpointer. And this will allow to keep track of multi‑thread conversations and to share history between agents of the sessions. And we're going to do the same also now for the conversation buffer, which is also used to set up short‑term memory, and we're going to use that to actually keep track of entries and memories using a state. Because with LangGraph, you have a built‑in feature that allows to share a reusable state to define and update the state. So, let's copy these lines right here, and go back to utils again, and go back to the section where we are setting up short‑term memory.
LangGraph Long-term Memory Persistence
So now let's see how to enable long‑term memory persistence and use semantic search to retrieve data and knowledge using a ChromaDB vector store. So when we go back to the project, you're going to see that we already have a ChromaDB vector store which is set up, and this is to integrate RAG. Basically, what we do is to allow these multi‑agent systems to retrieve data by using semantic search in order to generate augmented responses using context because we are building a multi‑agent system that can perform tasks based on specific guidelines and standards. So, let's go back. So what we do here in order to set up long‑term memory is also by setting up a vector store. And then we have some helper functions that we use to save to the memory, but also to retrieve from the memory. We're going to find the documentation here. And we actually keep track of the state at the graph level, but you can also do it at the agent level by also using a checkpointer. So this is a thread. What we do is to use a thread in order to keep track of the conversation states across session. So you're going to find it, so here you can actually add it in your graph, but you're going to find it in the tools where we add a checkpointer for every agent. So every agent is going to be aware of the context, any change in the state, and also retrieve past interactions, information, and memories using this checkpointer. So we're going to set this up. So for your convenience as well, you're going to find these helper functions already defined in the README file. So you can just find the sections for the long‑term memory, line 207. So here this is already set up. So we're going to copy from line 213 all the way down to 231. Let's go back to the utils file. All right, so what we do first is to allow to store to the memory by adding some memories, and then we persist in the Memorystore. We can also count the number of memories. And finally, we have this helper function to retrieve memories using the vector store. So, what we do is to return the memories, but also the memories_count. We're going to use that for testing later.
High Performance and Production-grade Scaling
So, things are moving very fast with the project. We now have short‑term and long‑term memory, but remember that we want to build a high‑performance, production‑grade, scalable AI system that can handle complex engineering workflows, concurrent requests, and FastAPI responses. So we need to build a system that can automate memory pruning with compression and summarization techniques. Cache responses to limit API calls to reduce cost and latency. So in your project, so below, from line 141, you're going to find some helper functions to summarize and compress memory when needed, and also another function, line 150, to prune long‑term memory. So, let's do that. We're going to start with this function, which is summarize and compress memory. So, as the name suggests, we want to compress the memory. For this one, it's going to be handled for the short‑term memory. It's going to take a text as an input and then return a summarized, generated content. So, it's going to be based on one condition, which is to count the number of characters. And if it is beyond 500, we want to then generate a summarized copy of this text in order to only keep the compressed version of this text, and then delete this text. It's going to be then saved at the thread level. Let's add it here. And below we also want to maintain long‑term memory by pruning the memory. So, let's go back. We're going to also find this function. So, it's going to be more complex here. So, basically what we do is to retrieve all these documents, and we're going to use this threshold, which is to count the maximum number of documents that we want to keep in the storage, which is going to be 100. So anything beyond that is going to be deleted, and for the rest, it's going to be summarized and stored in the storage. So we're going to store a compressed summary. All right, so let's copy all of that. So, starting from line 262 all the way to 302. Let's go back and then add it here. So we're going to continue with short‑term memory maintenance, and for this part, it is interesting because we're going to handle that at the graph level. And that's going to be used at the graph level to let the agents and the system decide when to trigger these actions, when we want to also optimize memory storage. If you go to the main and entry point of the project, you're going to find the workflow architecture, and there is this decision_router that allows to go to one path of the workflow based on the lead agent's outputs. So if it's approved, it's going to go through the node to summarize and prune memory, and then it's going to be the end of the workflow. But if the code review is rejected, it's going to go back to the start of the workflow. So we're going to add this node in order to allow to maintain any excess in memory. Basically, it's going to look for the short‑term memory, which is saved in the state, which has been maintained and retained at the graph level, and then summarize it in order to update the state again with a summarized and compressed version. Here as well, the purpose is to keep the memory to a minimum. So let's copy from line 307 all the way down to 319, and then add it here, like this. I'm going to go back. Then, do the same for long‑term memory. Then we have a summarizer node, which is going to be like a final step, which is to structure and then format a detailed and comprehensive output using the task, the query, the final decision, but also previous decision and conversation in relation with the final decision and query. So that's going to allow to keep track of a state which is more structured and detailed and comprehensive in order to make the retrieval even more effective across sessions. So let's copy from line 332 all the way down to 367. So here as well, it's going to be handled at the graph level. We're going to have a look at this. Again, let's go to main.py. You're going to see that now you have this Memory Nodes, and they're going to be triggered when the graph is executed. So they are added as new components to the graph, and then they are executed based on the core flow when we add edges. And it's going to be triggered based on the execution, but also based on the outputs during the execution of the flow. The purpose, again, is to maintain and optimize memory storage in order to keep it small, allow for an effective retrieval of past interactions, allow for FastAPI responses, a better control of costs and token usage, avoid any excess and overload of the language model's context window, and overall, so what we want ultimately, is to build high‑performance agentic applications with persistent memory.
High Performance and Production-grade Scaling – Part 2
And one last thing, we are also adding caching to allow to cache responses and avoid that language models are called more than once, more than necessary. Okay, so we're going to copy from line 381 all the way down to 395, and we're going to find this other file, which is tools, because this one is going to be enabled at the agent level, like a middleware, so it's going to be triggered based on the outputs or percepts. We're going to let the agents decide. Make decisions and take actions based on one output, and let the agents decide if it needs to call the language models or cache the responses. Check the caching before calling the language model. So, the action of caching is going to be automated. So, we're going to add this and then test it afterwards. And in the main file, you're going to find where the graph is built, and we're going to have a chance afterwards to test it, to run the workflow, but to test it before that to check that the agentic workflow is running as expected and efficiently.
Testing and Monitoring Performance
Now it's time to see some results. We're going to run some tests with some mock tests and also add observability to monitor performance, see how the application is working. Inside the project, you will find one test file. And we use this file to define some mock tests to validate the different features for short‑term and long‑term memory, but also for the summarization and pruning functions. So, we are writing some mock tests. We allow to store some fake messages that are deleted afterwards, but we also test if pruning is executed and triggered if needed. So we're going to be able to test this. And we also test caching. And you have the options to test just a few features, but also to run the tests, all the tests at once. You also have the option to test the engineering workflow. And so you also have the option to add observability with LangSmith. It works with LangChain, so very easy to set up. You need to create an account, set up API keys, and also add the environment variables. And you have all the information when you create a new project, you just need to follow the instructions on the screen. It is very simple and straightforward. And here we can access one project that I have created to trace the outputs and also the details for every output generated every time that I execute the graph. And, of course, I'm providing with instructions in order to get all sets in the README file. So now let's run the test, the mock test, to see how the graph is working. So we're going to run with python test.py, and we're going to see the outputs as it progresses. All right, so we're going to start with testing the engineering workflow. So it's going to go through one simple request and go through every stage of the code review. Next, it goes to testing the pruning and caching functions, and we're going to insert fake messages in order to test how many messages we have in the storage and see if the pruning functions are triggered as expected based on the conditions that we have defined. We have a sample of the retrieved memories, limited to 3, and we can also see the counts of the memories before pruning with a count of 224. So let's go back up because it was very fast. And after checking if we need to prune, we can see that the function was triggered and executed as expected in order to reduce the number of memories to 101. The test for the memory pruning passed and was completed successfully. Same for caching, in order to limit the number of calls that was triggered to interact with language models and allow to return the cached outputs instead of calling and invoking the language models more than once. So, this is essential to test, observe, and monitor the behavior of your AI workflows. So, this is always recommended to add some unit tests for memory validation and observability by using tools like LangSmith. As a next step toward production‑grade AI system, you have the options to integrate other and additional high‑performance infrastructure. For example, you have Redis, which is an ultrafast in‑memory data store, useful for caching session storage, real‑time data access, but also for handling large datasets and high traffic. You have Pinecone, which is a fully‑managed vector database optimized for large‑scale semantic search; Weaviate, an open‑source vector database with hybrid search; PostgreSQL, a reliable relational database enhanced with vector search also; and finally, Elasticsearch, a powerful search and analytics engine that can support hybrid retrieval. So, these are solutions, to name a few, that can help improve performance, scalability, reliability, and flexibility in your AI agentic system.